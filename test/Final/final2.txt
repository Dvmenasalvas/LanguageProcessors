#Prueba 2
#Fragmento de código en el que se declara un array con varios campos, unos inicializados y otros no.
#Posteriormente, se utiliza un switch para bien, imprimir por pantalla o acceder a los campos del
#struct declarado previamente


struct myStruct{
    int nombre;
    boolean b;
    boolean b2 = true;
};
int x = 10;
struct myStruct persona;

switch (x) {
    case 1 {
        print(x1);
    }
    case 2 {
        print(x2);
    }
    default{
        persona.nombre = FernandoAlonso;
        persona.b = false;
    }
}